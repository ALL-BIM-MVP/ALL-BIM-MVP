// Códigos estables de los módulos (modules.code en database/system-data.sql)
// — el nombre visible ("METRADOS BIM") puede cambiar, el código no.
// Usado por los archivos que genera el propio backend (Excel exportado,
// .frag, IFC subido), que fijan su módulo en el servidor.
export const METRADOS_MODULE_CODE = "metrados";
